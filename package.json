{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --parallel",
    "start:frontend": "npm run --prefix ./packages/frontend/ start",
    "start:backend": "npm run --prefix ./packages/backend/ start"
  },
  "devDependencies": {
    "lerna": "^6.0.3"
  }
}